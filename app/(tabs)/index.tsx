import { StyleSheet, Text, View } from "react-native";
import React from "react";
import ColorList from "@/components/color-list";

const Home = () => {
  return (
    <View>
      <ColorList color="#0c0c0d" />
    </View>
  );
};

export default Home;

const styles = StyleSheet.create({});
